<resources xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Base application theme. -->
    <style name="Base.Theme.SmartCloudAndroidClient" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SmartCloudAndroidClient" parent="Base.Theme.SmartCloudAndroidClient"/>

    <style name="MyToolbarTheme" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="android:textSize">10sp</item>
        <item name="iconTint">@color/lightGray</item>
        <item name="android:background">@color/cardview_dark_background</item>
    </style>

    <style name="MyRadioTheme" parent="Widget.Material3.CompoundButton.RadioButton">
        <item name="android:button">@null</item>
        <item name="android:gravity">left|center_vertical</item>
        <item name="android:drawableRight">@android:drawable/btn_radio</item>
    </style>

</resources>
